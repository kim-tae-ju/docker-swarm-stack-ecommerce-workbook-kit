api:
  dashboard: true
entryPoints:
  web:
    address: ":80"
providers:
  swarm:
    endpoint: "unix:///var/run/docker.sock"
    watch: true
    exposedByDefault: false
log:
  level: INFO
accessLog: {}
